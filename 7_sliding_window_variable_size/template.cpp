#include<iostream>
using namespace std;

void solve(vector<int> &arr){
    int n=arr.size();
    int left = 0;

    for (int right = 0; right < n; right++) {

        // Add nums[right]

        while (condition is invalid) {

            // Remove nums[left]
            left++;
        }

        // Update answer
    }
}

int main(){

    

    return 0;
}